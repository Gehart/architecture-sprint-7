apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-back-end-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-back-end-api
  template:
    metadata:
      labels:
        app: admin-back-end-api
        role: admin-back-end-api
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: admin-back-end-api-service
spec:
  selector:
    app: admin-back-end-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
